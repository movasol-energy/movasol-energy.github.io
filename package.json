{
  "name": "movasol_website",
  "version": "1.0.0",
  "scripts": {
  "build:sass":  "sass docs/assets/scss:docs/assets/css",
  "build:render":"python3 render.py",
  "build":       "npm run build:sass && npm run build:render",
  "start":       "npm run build && npx serve docs"
},
  "devDependencies": {
    "sass": "^1.x",
    "serve": "^14.2.4"
  }
}
